## Repo Notes

- Angular 19 app generated with Angular CLI 19.2.26 and npm; verify builds with `npm run build`.
- Tests use the default Angular CLI Karma setup via `npm test`, which requires a browser environment.
